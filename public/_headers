# Cloudflare static-asset response headers.
# https://developers.cloudflare.com/workers/static-assets/headers/

# Service Worker scripts: force revalidation on every request so a deploy's
# cache-versioning fix (see public/sw.js, public/sw-cache-utils.js) is never
# itself stuck behind a stale cached copy of the Worker script.
/sw.js
  Cache-Control: no-cache
/sw-cache-utils.js
  Cache-Control: no-cache

/*
  # RFC 8288 Link headers for agent/crawler discovery (real, existing resources only).
  Link: </sitemap.xml>; rel="sitemap"; type="application/xml"
  Link: </privacy>; rel="privacy-policy"
  # Security headers.
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  X-Frame-Options: SAMEORIGIN
  Permissions-Policy: geolocation=(), microphone=(), camera=(), interest-cohort=()
  Cross-Origin-Opener-Policy: same-origin
  # Content-Security-Policy. ENFORCED (was Report-Only during allowlist
  # validation). Directives are unchanged from the validated report-only policy,
  # so what was being reported is now what is enforced. The allowlist covers
  # Google Tag/Analytics, the AdSense ad ecosystem (googlesyndication /
  # doubleclick / adtrafficquality), and Cloudflare Turnstile. `script-src` needs
  # 'unsafe-inline' because GA/Consent Mode + AdSense inject inline scripts
  # (nonces can't cover ad-injected code); `img-src https:` allows ad-creative
  # images from arbitrary advertiser hosts. If a legitimate resource is ever
  # blocked, add its host here (or temporarily switch back to
  # `Content-Security-Policy-Report-Only` to re-observe).
  Content-Security-Policy: default-src 'self'; base-uri 'self'; object-src 'none'; frame-ancestors 'self'; form-action 'self'; upgrade-insecure-requests; script-src 'self' 'unsafe-inline' https://www.googletagmanager.com https://pagead2.googlesyndication.com https://*.googlesyndication.com https://partner.googleadservices.com https://tpc.googlesyndication.com https://www.google-analytics.com https://adservice.google.com https://*.adtrafficquality.google https://challenges.cloudflare.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self'; frame-src https://googleads.g.doubleclick.net https://tpc.googlesyndication.com https://www.google.com https://*.adtrafficquality.google https://challenges.cloudflare.com; connect-src 'self' https://www.googletagmanager.com https://www.google-analytics.com https://*.google-analytics.com https://*.analytics.google.com https://pagead2.googlesyndication.com https://*.googlesyndication.com https://*.g.doubleclick.net https://*.adtrafficquality.google https://challenges.cloudflare.com; worker-src 'self' blob:
